# --- CORE APPLICATION SETTINGS ---

# Defines the environment mode. Set to 'production' for building and testing final artifacts.
# Default: development
VITE_NODE_ENV=production

# --- AURAFLUX NEXUS API CONFIGURATION ---

# Base URL for the Auraflux-Nexus REST API.
# Example: https://api.auraflux.com/v1
VITE_AURAFLUX_NEXUS_URL=yourBackendURL

# Base URL for the Auraflux-Nexus WebSocket service.
# Example: wss://ws.auraflux.com/v1
VITE_AURAFLUX_WS_URL=yourWebSocketURL

# --- WEBSOCKET RECONNECTION STRATEGY (Advanced Configuration) ---
# These settings control the client-side exponential backoff strategy for WebSocket reconnection attempts.

# Maximum number of times the client will attempt to reconnect after a disconnect.
# Default: 10
VITE_WS_MAX_RECONNECT_ATTEMPTS=yourWebSockerMaxReconnectAttempts

# Initial delay (in milliseconds) before the first reconnection attempt.
# Default: 1000 (1 second)
VITE_WS_INITIAL_BACKOFF_TIME_MS=yourWebSocketInitialBackoffTime

# Maximum allowed delay (in milliseconds) between reconnection attempts to prevent excessive traffic.
# Default: 30000 (30 seconds)
VITE_WS_MAX_BACKOFF_TIME_MS=yourWebSocketMaxBackoffTime